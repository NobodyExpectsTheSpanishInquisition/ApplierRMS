version: "3.8"

services:
  database:
    env_file:
      - .env
    build:
      context: .
      dockerfile: dockerfile.database
      args:
        - DATABASE_IMAGE_TAG=${DATABASE_IMAGE_TAG}
    container_name: ${DATABASE_CONTAINER_NAME}
    ports:
      - "5432:5432"

  accounts:
    env_file:
      - .env
    build:
      context: .
      dockerfile: dockerfile.accounts
      args:
        - IMAGE_TAG=${ACCOUNTS_IMAGE_TAG}
    container_name: ${ACCOUNTS_CONTAINER_NAME}
    depends_on:
      - database
    volumes:
      - ./config/php.ini.example:/usr/local/etc/php/php.ini:ro
      - ./config/xdebug.ini.example:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini
      - ./config/error_reporting.ini:/usr/local/etc/php/conf.d/error_reporting.ini
      - ./api/accounts:/var/www/html/api:rw
      - ./logs/accounts/php.log:/var/log/fpm-php.www.log

  nginx:
    env_file:
      - .env
    build:
      context: .
      dockerfile: dockerfile.nginx
      args:
        - IMAGE_TAG=${NGINX_IMAGE_TAG}
    container_name: ${NGINX_CONTAINER_NAME}
    ports:
      - ${NGINX_ACCOUNTS_PORT}:80
    volumes:
      - ./logs/nginx:/var/log/nginx/:rw
      - ./api/accounts/:/var/www/html/api/:rw
      - ./config/default.conf.example:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - accounts
